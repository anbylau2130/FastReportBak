(function () {
	usp.namespace("usp.operator.changepassword");
	//usp.operator.changepassword.checkrepassword = function () {
	//	var oldPassword = $("#oldPassword").val();
	//	var newPassword = $("#newPassword").val();
	//	var confirmPassword = $("#confirmPassword").val();
	//	if (newPassword != confirmPassword) {
	//		$.messager.alert('提示信息', '输入密码不一致');
	//	    return false;
	//	} else {
	//		//location.href = '/System/Operator/Audit?oldPassword=' + oldPassword + '&newPassword' + newPassword;
	//	    return true;
	//	}
	
})
(this);